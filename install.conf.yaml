# Only 4 spaces!

- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    ~/.config/nvim: nvim
    ~/.config/hypr: arch/hypr
    ~/.config/lazygit: lazygit
    ~/.config/lazydocker: lazydocker
    ~/.config/ghostty: arch/ghostty
    ~/.config/waybar: arch/waybar
    ~/.config/fsel: arch/fsel
    ~/.config/btop/btop.conf: arch/btop.conf
    ~/.zshrc: arch/.zshrc
